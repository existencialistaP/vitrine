/**
 * Marcador de objeto de domínio. Todo elemento do domínio (entidade, valor,
 * evento, ID) implementa esta interface.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface DomainObject {}
